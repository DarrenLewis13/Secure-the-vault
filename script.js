// 3 variables that each add up to 10,40, and 39.
const first = 7 + 3;
const second = 120/3;
const third = (second - 1)
// Display the combination in an alert pop-up
const combination = first + '-' + second + '-' + third;
const userString= "you have received this message because you have been chosen to open an important vault. Here is the secret combination:"
alert(userString + ' ' + combination);